services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CORS_ALL=false
      - PRICEIQ_DB=/data/priceiq.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
